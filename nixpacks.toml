[phases.setup]
nixPkgs = ['nodejs', 'python312']

[phases.install]
cmds = [
    'cd frontend && npm install && npm run build && cd ..',
    'pip install -r requirements.txt'
]

[start]
cmd = 'uvicorn backend.app:app --host 0.0.0.0 --port $PORT'
